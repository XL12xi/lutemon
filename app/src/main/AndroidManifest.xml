<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.example.lutemon">
    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher" android:label="@string/app_name" android:roundIcon="@mipmap/ic_launcher_round" android:supportsRtl="true" android:theme="@style/Theme.Lutemon">
        <!--  主界面  -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--  其他活动页面  -->
        <activity android:name=".CreateLutemonActivity"/>
        <activity android:name=".HomeActivity"/>
        <activity android:name=".TrainingActivity"/>
        <activity android:name=".BattleActivity"/>
        <!--  新增的战斗界面  -->
        <activity android:name=".StatsActivity"/>
    </application>
</manifest>